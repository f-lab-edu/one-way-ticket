spring:
  datasource:
    url: jdbc:mysql://db:3306/my-db
    username: tester
    password: 1234
    schema: classpath:/schema.sql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
